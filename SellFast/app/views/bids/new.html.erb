<h2>Enter Your Bidding price</h2>

<%= form_for @bid, url: item_bids_path(@item.id), html: { method: :post } do |f| %>

  <div class="field">
    <%= f.label :amount %><br />
    <%= f.text_field :amount, autofocus: true, autocomplete: "Item name" %>
  </div>

  <%= f.submit "Create"%>

<% end %>
